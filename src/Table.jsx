import React from "react";

export default function Table(){


    return(
         <div className="table">
            {
                aluno.map(key => id)
            }
         </div>
    );
    }
